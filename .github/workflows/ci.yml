name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: cv-lab/backend/requirements.txt

      - name: Install dependencies
        working-directory: cv-lab/backend
        run: |
          python -m pip install --upgrade pip
          pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt

      - name: Byte-compile (syntax check)
        working-directory: cv-lab/backend
        run: python -m compileall app

      - name: Analyzer smoke test
        working-directory: cv-lab/backend
        env:
          DEVICE: cpu
          STORAGE_BACKEND: local
        run: python ../selftest.py

  # Build & push a container image to GHCR (uncomment and adjust to enable).
  #
  # publish-image:
  #   needs: smoke-test
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     packages: write
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: docker/login-action@v3
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
  #     - uses: docker/build-push-action@v6
  #       with:
  #         context: cv-lab
  #         file: cv-lab/backend/Dockerfile
  #         push: true
  #         tags: ghcr.io/${{ github.repository }}:latest
